# Deployment Guide

## Vercel Deployment

1. Import project from GitHub repository
2. In project settings, set **Root Directory** to `web`
3. Deploy

The `vercel.json` file in the root directory configures the build automatically.

